# No credentials belong in the frontend.
# Node preview: public/runtime.js selects browser simulation.
# Flask-served preview: Flask overrides /runtime.js and selects /api/v1.
# Set AI credentials only in backend/.env.
