# Bounded input requirements, NOT a resolved lockfile.
# Resolve and audit a lockfile in the team's connected build environment.
Flask>=3.1.3,<3.2
SQLAlchemy>=2.0.50,<2.1
alembic>=1.18.4,<2
Flask-WTF>=1.2.2,<2
Flask-Limiter[redis]>=3.12,<5
pydantic>=2.13.4,<3
argon2-cffi>=25.1,<26
PyMySQL[rsa]>=1.1,<2
python-dotenv>=1,<2
waitress>=3,<4
tzdata>=2025.2
