# Local example only. Copy to infra/.env.local, then replace all values.
# Use URL-safe random hex for MYSQL_PASSWORD in this sample DSN.
MYSQL_PASSWORD=replace_with_random_hex
MYSQL_ROOT_PASSWORD=replace_with_different_random_hex
SECRET_KEY=replace_with_64_random_hex_characters
