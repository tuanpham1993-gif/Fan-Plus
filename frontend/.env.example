# The integration client uses relative /api/v1 paths.
# Vite development proxy target is in vite.config.ts.
# No API keys or authentication secrets belong in frontend environment variables.
# The local repository is still active; there is no automatic API-mode switch.
