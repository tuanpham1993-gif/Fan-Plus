import React, { useMemo, useState } from 'react';
import { useApp } from '../lib/store';
import { Link, useLocation, navigate } from '../lib/router';
import { distanceKm, calendarFile, safeExternalUrl } from '../domain/logic';
import { Icon, Button, Crumbs, PageHeading, Notice, Empty, downloadFile } from '../components/ui';
const date = (s: string, opts: Intl.DateTimeFormatOptions = { dateStyle: 'medium' }) => new Intl.DateTimeFormat('en-GB', { ...opts, timeZone: 'Asia/Ho_Chi_Minh' }).format(new Date(s));
export default function Events({ id }: {
    id?: string;
}) {
    const { db, notify } = useApp();
    const { params } = useLocation();
    const [view, setView] = useState('list'), [geo, setGeo] = useState<{
        lat: number;
        lng: number;
    } | null>(null), [geoBusy, setGeoBusy] = useState(false), [geoError, setGeoError] = useState(''), [onlineMap, setOnlineMap] = useState(false), [selected, setSelected] = useState('e1'), [month, setMonth] = useState(new Date(Date.UTC(2026, 9, 1)));
    const city = params.get('city') || '', radius = Number(params.get('radius') || 0);
    const events = useMemo(() => { const items = (db?.events || []).filter(e => !city || e.city === city).map(e => ({ ...e, distance: geo ? distanceKm(geo, e) : null })).filter(e => !geo || !radius || e.distance! <= radius); return items.sort((a, b) => geo ? a.distance! - b.distance! : Date.parse(a.startsAt) - Date.parse(b.startsAt)); }, [db, city, geo, radius]);
    if (!db)
        return null;
    const update = (key: string, value: string) => { const p = new URLSearchParams(params); value ? p.set(key, value) : p.delete(key); navigate('/events' + (p.size ? '?' + p : '')); };
    const locate = () => { if (!navigator.geolocation) {
        setGeoError('Geolocation is unavailable. Please select a city.');
        return;
    } setGeoBusy(true); setGeoError(''); navigator.geolocation.getCurrentPosition(p => { setGeo({ lat: p.coords.latitude, lng: p.coords.longitude }); setGeoBusy(false); update('city', ''); notify('Events are sorted by distance. Your coordinates are not saved.', 'info'); }, err => { setGeoBusy(false); setGeoError(err.code === 1 ? 'Location permission was denied. You can still browse by city.' : 'Location could not be determined. Try again or choose a city.'); }, { enableHighAccuracy: false, timeout: 8000, maximumAge: 60000 }); };
    if (id) {
        const event = db.events.find(e => e.id === id);
        if (!event)
            return <Empty title="Event not found" description="This event may have been removed."><Link className="btn btn-primary" to="/events">Browse events</Link></Empty>;
        return <><Crumbs items={[{ label: 'Events', to: '/events' }, { label: event.title }]}/><div className="event-detail"><img src={event.image} alt="Original event concept artwork" width="1280" height="900"/><div><span className="eyebrow">A FICTIONAL COMMUNITY EVENT</span><h1>{event.title}</h1><p>{event.description}</p><div className="event-detail-facts"><p><Icon name="calendar"/>{date(event.startsAt)}</p><p><Icon name="clock"/>{date(event.startsAt, { timeStyle: 'short' })} - {date(event.endsAt, { timeStyle: 'short' })} (Asia/Ho_Chi_Minh)</p><p><Icon name="pin"/>{event.venue}, {event.city}</p></div><Button onClick={() => downloadFile('fanhub-demo-' + event.id + '.ics', calendarFile(event), 'text/calendar;charset=utf-8')}><Icon name="download" size={18}/>Add demo event to calendar</Button>{event.ticketUrl && safeExternalUrl(event.ticketUrl) ? <a className="btn btn-secondary" href={safeExternalUrl(event.ticketUrl)!} target="_blank" rel="noopener noreferrer">Visit organizer's ticket page <Icon name="external" size={16}/></a> : <p className="caption">No real tickets are available for this fictional event.</p>}<Notice>Do not travel to this location based on the demo. Event name, venue and schedule are test data.</Notice></div></div><Link className="text-link" to={'/events?city=' + encodeURIComponent(event.city)}>More events in {event.city}<Icon name="arrow" size={17}/></Link></>;
    }
    const mapEvent = events.find(e => e.id === selected) || events[0];
    const days = new Date(Date.UTC(month.getUTCFullYear(), month.getUTCMonth() + 1, 0)).getUTCDate(), offset = (month.getUTCDay() + 6) % 7;
    const marker = mapEvent ? `${mapEvent.lat},${mapEvent.lng}` : '';
    const bbox = mapEvent ? `${mapEvent.lng - .055},${mapEvent.lat - .035},${mapEvent.lng + .055},${mapEvent.lat + .035}` : '';
    return <><Crumbs items={[{ label: 'Events' }]}/><PageHeading eyebrow="BEYOND THE SCREEN" title="Find your people." description="The best part of a fandom? Sharing it. Discover gatherings, workshops and worlds close to you."><Button variant="secondary" busy={geoBusy} onClick={locate}><Icon name="pin" size={17}/>{geo ? 'Refresh my location' : 'Use my location'}</Button></PageHeading><Notice>All events below are fictional test data. Location is requested only when you choose it, kept in memory and never saved. Online maps contact OpenStreetMap only after you load them.</Notice>{geoError && <div className="form-error" role="alert">{geoError}</div>}<div className="event-toolbar"><div><label className="sr-only" htmlFor="city">Filter events by city</label><select id="city" value={city} onChange={e => update('city', e.target.value)}><option value="">All cities</option>{[...new Set(db.events.map(e => e.city))].map(c => <option key={c}>{c}</option>)}</select>{geo && <><select aria-label="Distance radius" value={radius} onChange={e => update('radius', e.target.value)}><option value={0}>Any distance</option><option value={25}>Within 25 km</option><option value={100}>Within 100 km</option><option value={500}>Within 500 km</option></select><button className="small-link" onClick={() => { setGeo(null); update('radius', ''); }}>Forget location</button></>}</div><div className="segmented" role="group" aria-label="Event view">{[['list', 'list', 'List'], ['calendar', 'calendar', 'Calendar'], ['map', 'pin', 'Map']].map(([v, icon, label]) => <button key={v} className={view === v ? 'active' : ''} aria-pressed={view === v} onClick={() => setView(v)}><Icon name={icon} size={16}/>{label}</button>)}</div></div>{view === 'list' && (events.length ? <div className="event-list">{events.map(e => <article className="event-card" key={e.id}><img src={e.image} alt="" width="280" height="180"/><div className="event-date"><strong>{date(e.startsAt, { day: '2-digit' })}</strong><span>{date(e.startsAt, { month: 'short' })}</span></div><div className="event-card-body"><span className="eyebrow">{db.categories.find(c => c.id === e.categoryId)?.name} / DEMO EVENT</span><h2><Link to={'/events/' + e.id}>{e.title}</Link></h2><p>{e.description}</p><div className="event-meta"><span><Icon name="pin" size={14}/>{e.city}</span><span><Icon name="clock" size={14}/>{date(e.startsAt, { timeStyle: 'short' })} GMT+7</span>{e.distance !== null && <span>{e.distance.toFixed(1)} km away</span>}</div></div><Link className="icon-btn event-arrow" aria-label={'View ' + e.title} to={'/events/' + e.id}><Icon name="arrow"/></Link></article>)}</div> : <Empty icon="pin" title="No events in this orbit" description="Try another city or a larger distance radius."><Button onClick={() => { setGeo(null); navigate('/events'); }}>Reset event filters</Button></Empty>)}{view === 'calendar' && <div className="calendar-panel"><div className="calendar-header"><Button variant="ghost" onClick={() => setMonth(new Date(Date.UTC(month.getUTCFullYear(), month.getUTCMonth() - 1, 1)))}>Previous month</Button><h2>{new Intl.DateTimeFormat('en-GB', { month: 'long', year: 'numeric', timeZone: 'UTC' }).format(month)}</h2><Button variant="ghost" onClick={() => setMonth(new Date(Date.UTC(month.getUTCFullYear(), month.getUTCMonth() + 1, 1)))}>Next month</Button></div><p className="caption">Event dates shown in Asia/Ho_Chi_Minh (GMT+7).</p><div className="calendar-grid">{['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map(d => <div className="calendar-weekday" key={d}>{d}</div>)}{Array.from({ length: offset }, (_, i) => <div key={'empty' + i} className="calendar-day calendar-blank"/>)}{Array.from({ length: days }, (_, i) => { const key = `${month.getUTCFullYear()}-${String(month.getUTCMonth() + 1).padStart(2, '0')}-${String(i + 1).padStart(2, '0')}`; return <div className="calendar-day" key={key}><span>{i + 1}</span>{events.filter(e => new Intl.DateTimeFormat('en-CA', { year: 'numeric', month: '2-digit', day: '2-digit', timeZone: 'Asia/Ho_Chi_Minh' }).format(new Date(e.startsAt)) === key).map(e => <Link to={'/events/' + e.id} key={e.id}>{e.title}</Link>)}</div>; })}</div></div>}{view === 'map' && <div className="map-layout"><div className="map-list">{events.map(e => <button key={e.id} onClick={() => setSelected(e.id)} className={mapEvent?.id === e.id ? 'active' : ''}><strong>{e.title}</strong><span>{e.city}</span></button>)}</div><div className="map-canvas">{!mapEvent ? <Empty title="No events to map" description="Choose another city or remove the distance filter."/> : onlineMap ? <><iframe title={'OpenStreetMap location of ' + mapEvent.title} src={`https://www.openstreetmap.org/export/embed.html?bbox=${encodeURIComponent(bbox)}&layer=mapnik&marker=${encodeURIComponent(marker)}`} loading="lazy" referrerPolicy="strict-origin-when-cross-origin"/><div className="map-caption"><span>Fictional venue / {mapEvent.city}</span><Link to={'/events/' + mapEvent.id}>View event <Icon name="arrow" size={15}/></Link></div></> : <div className="map-consent"><Icon name="globe" size={54}/><h2>Your next connection, mapped.</h2><p>Load a live OpenStreetMap for the selected fictional event. Your browser will contact an external map provider.</p><Button onClick={() => setOnlineMap(true)}>Load online map <Icon name="external" size={16}/></Button><small>Internet required. Event list and calendar work offline.</small></div>}</div></div>}</>;
}
