import React, { useEffect, useState } from 'react';
import { useApp } from '../lib/store';
import { Link, navigate } from '../lib/router';
import { repository } from '../services/repository';
import { Icon, Button, Crumbs, ContentCard, BookmarkButton, Empty, Notice, Modal, copyText, typeLabels } from '../components/ui';
export function RichText({ text }: {
    text: string;
}) { return <div className="prose">{text.split(/\n\s*\n/).map((block, i) => { if (block.startsWith('## '))
    return <h2 key={i}>{block.slice(3)}</h2>; const bits = block.split(/(\*\*[^*]+\*\*)/g); return <p key={i}>{bits.map((t, j) => t.startsWith('**') ? <strong key={j}>{t.slice(2, -2)}</strong> : <React.Fragment key={j}>{t}</React.Fragment>)}</p>; })}</div>; }
export default function Detail({ id }: {
    id: string;
}) {
    const { db, user, spoilerSafe, perform, notify } = useApp();
    const [revealed, setRevealed] = useState(false), [gallery, setGallery] = useState<number | null>(null), [shareOpen, setShareOpen] = useState(false), [mediaError, setMediaError] = useState(false), [ratingBusy, setRatingBusy] = useState(false);
    const content = db?.contents.find(c => c.id === id && (c.status === 'published' || user?.role === 'admin'));
    useEffect(() => { setRevealed(false); setMediaError(false); if (user && content)
        void perform(() => repository.recordView(id)); }, [id, user?.id]);
    if (!db || !content)
        return <Empty title="This story is unavailable" description="It may have been removed or may not be published yet."><Link className="btn btn-primary" to="/explore">Back to Explore</Link></Empty>;
    const category = db.categories.find(c => c.id === content.categoryId), related = db.contents.filter(c => c.id !== id && c.categoryId === content.categoryId && c.status === 'published').slice(0, 3), myRating = db.ratings.find(r => r.userId === user?.id && r.contentId === id)?.value || 0, ratings = db.ratings.filter(r => r.contentId === id);
    const galleryImages = [content.image, '/art/community.svg', '/art/manga.svg'];
    const share = async () => { const url = window.location.href; if (navigator.share) {
        try {
            await navigator.share({ title: content.title, url });
            return;
        }
        catch (e) {
            if (e instanceof Error && e.name === 'AbortError')
                return;
        }
    } if (await copyText(url))
        notify('Story link copied.');
    else
        setShareOpen(true); };
    return <><Crumbs items={[{ label: 'Explore', to: '/explore' }, { label: category?.name || 'Community', to: '/explore?category=' + content.categoryId }, { label: typeLabels[content.type] }]}/><div className="detail-banner"><img src={content.image} alt={`Original ${content.fandom} concept illustration`} width="1280" height="900"/><div className="detail-banner-bottom"><span className="pill">{category?.name}</span><span>{content.fandom}</span><span className="pill pill-outline">{typeLabels[content.type]}</span></div></div><div className="detail-layout"><article><div className="detail-title"><span className="eyebrow">{content.status === 'draft' ? 'UNPUBLISHED DRAFT - ADMIN PREVIEW' : 'FROM THE FAN HUB COLLECTION'}</span><h1>{content.title}</h1><p className="detail-deck">{content.description}</p><div className="byline"><span className="avatar avatar-small">FH</span><span><strong>{content.author}</strong><br /><small>{new Intl.DateTimeFormat('en-GB', { dateStyle: 'medium', timeZone: 'Asia/Ho_Chi_Minh' }).format(new Date(content.publishedAt))} / {content.duration}</small></span><button className="icon-btn" onClick={share} aria-label="Share this story"><Icon name="share"/></button></div></div>{content.type === 'character' && <div className="character-facts"><div><span>Universe</span><strong>{content.fandom}</strong></div><div><span>Genre</span><strong>{content.genre}</strong></div><div><span>Demo year</span><strong>{content.year}</strong></div></div>}{content.type === 'video' && <div className="media-player"><video key={id} controls preload="metadata" poster={content.image} onError={() => setMediaError(true)}><source src={content.mediaUrl} onError={() => setMediaError(true)} type={content.mediaUrl?.split('?')[0].endsWith('.mp4') ? 'video/mp4' : 'video/webm'}/><track kind="captions" src="/media/portal.vtt" srcLang="en" label="English" default/>Your browser does not support this video.</video><p className="caption">Original six-second animation. Captions available. No third-party footage.</p></div>}{content.type === 'audio' && <div className="audio-player"><span className="audio-icon"><Icon name="music" size={38}/></span><div><h2>Orbit: an ambient sketch</h2><audio controls preload="metadata" src={content.mediaUrl} onError={() => setMediaError(true)}>Your browser does not support audio.</audio><p className="caption">Transcript: eight seconds of instrumental synthesized tones. No speech.</p></div></div>}{mediaError && <Notice kind="error">The media could not load. Check that the local media files are included, then refresh.</Notice>}{['gallery', 'merchandise'].includes(content.type) && <><h2 className="section-small-title">A closer look</h2><div className="gallery-grid">{galleryImages.map((src, i) => <button key={i} className="gallery-item" onClick={() => setGallery(i)} aria-label={`Open concept image ${i + 1}`}><img src={src} alt={`Original concept study ${i + 1}`} width="640" height="450"/><span><Icon name="plus" size={18}/></span></button>)}</div></>}{content.type === 'merchandise' && <Notice>This is a fictional showcase concept. There is no purchase, payment, preorder transaction or delivery service.</Notice>}{content.spoiler && spoilerSafe && !revealed ? <div className="spoiler-gate"><Icon name="shield" size={38}/><h2>A little heads-up.</h2><p>This section discusses a story reveal. Your spoiler-safe preference is on.</p><Button variant="secondary" onClick={() => setRevealed(true)}>I'm ready - reveal this section</Button></div> : <RichText text={content.body}/>}<div className="tag-list">{content.tags.map(t => <span className="chip static-chip" key={t}>{t}</span>)}</div><section className="rating-panel"><div><h2>Worth the discovery?</h2><p>{ratings.length ? `${ratings.length} demo rating${ratings.length > 1 ? 's' : ''} / ${(ratings.reduce((s, r) => s + r.value, 0) / ratings.length).toFixed(1)} average` : 'Be the first to rate this entry in your demo workspace.'}</p></div><div role="group" aria-label="Rate this content" className="stars">{[1, 2, 3, 4, 5].map(n => <button key={n} disabled={ratingBusy} aria-label={`Rate ${n} out of 5`} aria-pressed={myRating === n} onClick={async () => { if (!user) {
        navigate('/login?next=' + encodeURIComponent('/content/' + id));
        return;
    } setRatingBusy(true); await perform(() => repository.rate(id, n), 'Your rating has been saved.'); setRatingBusy(false); }}><Icon name="star" size={25} fill={n <= myRating ? 'currentColor' : 'none'}/></button>)}</div></section></article><aside className="detail-sidebar"><div className="panel"><span className="eyebrow">MAKE ROOM FOR A NEW FAVORITE</span><h2>Keep this world close.</h2><p>Save it for later. Add a note. Pick up where curiosity left off.</p><BookmarkButton content={content} compact={false}/><Button variant="ghost" onClick={share}><Icon name="share" size={17}/>Share this discovery</Button></div><div className="panel source-panel"><Icon name="shield" size={24}/><h3>Know what you're reading</h3><p>{content.sourceLabel}. This entry is for interface testing, not a verified reference about a real franchise.</p><Link to="/privacy" className="small-link">Content & privacy notes <Icon name="arrow" size={13}/></Link></div><Link to={'/explore?fandom=' + encodeURIComponent(content.fandom)} className="world-callout"><span>MORE FROM</span><h3>{content.fandom}</h3><Icon name="arrow"/></Link></aside></div>{related.length > 0 && <section className="content-section"><div className="section-heading"><h2>Stay a little longer<span className="accent">.</span></h2><Link to={'/explore?category=' + content.categoryId} className="text-link">Explore this world <Icon name="arrow" size={17}/></Link></div><div className="card-grid">{related.map(c => <ContentCard content={c} key={c.id}/>)}</div></section>}<Modal title="Concept gallery" open={gallery !== null} onClose={() => setGallery(null)} wide>{gallery !== null && <><img className="lightbox-image" src={galleryImages[gallery]} alt={`Original concept study ${gallery + 1}`}/><div className="modal-actions"><Button variant="secondary" onClick={() => setGallery((gallery + 2) % 3)}>Previous</Button><span>{gallery + 1} / 3</span><Button onClick={() => setGallery((gallery + 1) % 3)}>Next</Button></div></>}</Modal><Modal title="Share this discovery" open={shareOpen} onClose={() => setShareOpen(false)}><label className="field"><span>Copy this address</span><input readOnly value={window.location.href} onFocus={e => e.currentTarget.select()}/></label></Modal></>;
}
